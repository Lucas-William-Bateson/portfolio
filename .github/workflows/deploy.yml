name: Deploy

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, closed]
  schedule:
    - cron: '0 6 * * 1' # Weekly rebuild — Monday 6am UTC

jobs:
  deploy:
    uses: Lucas-William-Bateson/actions/.github/workflows/dokku-deploy.yml@main
    with:
      app: portfolio
      dockerfile: docker/Dockerfile
      port: '3000'
    secrets: inherit
