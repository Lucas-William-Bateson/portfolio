# portfolio secrets — managed via Proton Pass CLI
# Regenerate: pass-cli inject secrets.env.template -o secrets.env

IMAGE_FULL={{ pass://Secrets management/portfolio/image_full }}
CONTAINER_NAME=portfolio
APP_NAME=portfolio
ENVIRONMENT=production
IMAGE_TAG=latest
VERSION=latest
MAINTAINER_EMAIL={{ pass://Secrets management/portfolio/maintainer_email }}

HOST_PORT=3000
APP_PORT=3000

CPU_LIMIT=0.5
MEMORY_LIMIT=256M
MIN_CPU_LIMIT=0.1
MIN_MEMORY_LIMIT=64M

NETWORK_NAME=portfolio-network
